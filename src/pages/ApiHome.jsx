import React from "react";

export default function ApiHome() {
  return (
    <div>
      <h3>API Subdomain</h3>
      <p>
        This would be where API docs or JSON endpoints live (in production a
        separate server).
      </p>
    </div>
  );
}
